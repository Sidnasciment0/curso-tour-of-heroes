// Criando Interface do TypeScript

export interface Hero {
  //Propriedades
  id: number;
  name: string;
}
// Aqui temos a estrutura do módelo do Hero do nosso projeto.
// E para poder essa interface em outros lugares, tempos que fazer uma exportação = export.
